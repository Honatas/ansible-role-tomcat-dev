---
tomcat_version: 9.0.20
tomcat_linux_user: vagrant

tomcat_download_url: "https://archive.apache.org/dist/tomcat/tomcat-{{tomcat_version | basename}}/v{{tomcat_version}}/bin/apache-tomcat-{{tomcat_version}}.tar.gz"
tomcat_install_dir: "/home/{{tomcat_linux_user}}"
catalina_home: "{{tomcat_install_dir}}/{{tomcat_download_url | basename | regex_replace('.tar.gz', '')}}"
